<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ugo.android.gadsleaderboard">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LearningLeadersActivity">

        </activity>
        <activity android:name=".activity.GadsHomeActivity">

        </activity>
        <activity android:name=".activity.SingleFragmentActivity">

        </activity>
        <activity android:name=".activity.SkillIQLeadersActivity">

        </activity>
        <activity android:name=".activity.SubmitActivity"
            android:parentActivityName=".activity.GadsHomeActivity" >

        </activity>

        <activity android:name=".activity.SplashScreenActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>.40


    </application>

</manifest>